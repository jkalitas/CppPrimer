///* comment pairs /* */ cannot nest.
//* ''cannot nest'' is considered source code,
//    * as is the rest of the program
//*/

/*
NOTE: It's only possible to compile if we comment the comments itself,
otherwise e got syntax errors.
*/
int main() {
  return 0;
}
